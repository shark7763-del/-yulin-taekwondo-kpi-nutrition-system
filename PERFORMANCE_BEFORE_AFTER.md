# PERFORMANCE_BEFORE_AFTER.md

量測基準：`830跆拳道選手KPI後台.xlsx`（2026-08-30 匯出）
records = **1812 列 × 164 欄 = 297,168 格**，橫跨 2026-06-02 ~ 2026-08-30。

「讀取格數」= Apps Script 實際從 Sheet 讀進記憶體的儲存格數。
「getRange 次數」= 後端呼叫 `getRange().getValues()` 的次數（v79 被還原的教訓：**次數**才是瓶頸）。

## 教練後台開頁

| 情境 | 回傳列 | 讀取格數 | getRange 次數 | JSON | gzip | 頁數 |
|---|---:|---:|---:|---:|---:|---:|
| **修改前**：整表、全欄 | 1812 | 297,168 | 1 | **11.86 MB** | 1.14 MB | 6 |
| 中途 v84：45 天視窗、全欄 | 778 | 129,404 | 5 | 5.16 MB | 0.50 MB | 3 |
| **修改後**：45 天、87 欄白名單 | 778 | **129,404** | 5 | **2.22 MB** | 0.19 MB | **2** |

回應體積 **11.86 MB → 2.22 MB（19%）**，請求次數 **6 → 2**。

## 其他日常操作（本次新增的 bounded 查詢）

| 操作 | 修改前 | 修改後 | 回傳列 | 讀取格數 | JSON | 頁數 |
|---|---|---|---:|---:|---:|---:|
| 今日已回報名單 | 整表 11.86MB / 6 頁 | 14 天視窗 | 229 | 39,368 | **0.70 MB** | **1** |
| 本週之星 | 整表 11.86MB / 6 頁 | 本週（8 天） | 118 | 21,164 | **0.40 MB** | **1** |
| 上次表現（單一選手） | 整表 11.86MB / 6 頁 | `getRecentRecordsByName` | 該選手約 44 筆 | — | 約 0.4 MB | 1 |
| 出席報表 | 整表 11.86MB / 6 頁 | **完全不打後端**（用已抓到的當日資料） | 0 | 0 | **0** | **0** |

**教練重新整理一次，從「2 支無限制全表讀取（12 頁）」變成「1 支 bounded 讀取（2 頁）」。**

## 為什麼請求次數比體積更重要

手機端實際錯誤是 `Failed to fetch`，不是逾時或體積錯誤。
分頁把一次失敗的機率乘上頁數：單頁成功率 p，六頁全成功只有 p⁶。
p = 0.97 時，六頁 → 83%，兩頁 → 94%，一頁 → 97%。
**所以「少打幾次」比「每次小一點」對穩定性的貢獻更大**，這也是為什麼
出席報表那支請求直接被移除、而不是改小。

## 後端讀取策略：先定位、再讀內容

有日期視窗時，先只讀 `date` / `timestamp`（N × 2 格）定位，
再把連續的列合併成少數幾段讀內容：

| 視窗 | 符合列 | 段數（getRange） | 讀取格數 | vs 整表 297,168 |
|---|---:|---:|---:|---:|
| 近 7 天 | 92 | 1 | 15,088 | **6%** |
| 近 14 天 | 229 | 3 | 37,556 | **13%** |
| 近 45 天 | 778 | 4 | 127,592 | 44% |
| 近 90 天 | 1762 | 2 | 288,968 | 98% |

段數只有個位數，因為 records 近似 append（日期大致遞增）。

### 為什麼不用尾端切片
實測 1812 列有 **67 次逆序（3.70%）**——補填舊日期會 append 到尾端。
取 45 天視窗時，最後一筆不符合的列在第 1058 列，但真正符合的有 778 列，
其中 **25 列落在該點之前**。尾端切片會漏掉這 25 筆。
所以一律整欄掃 `date`（只有 N × 2 格，成本可忽略），**寧可多讀 index，不可漏資料**。

### 保險絲
若段數 > `RECORDS_MAX_RANGE_READS`（12），代表資料不是依時間排列
（例如做過一次依姓名排序的匯入）。這時分段讀會退化成幾十次 getRange，
正是 v79 被還原的原因。此時退回「一次讀完再過濾」，
最壞情況也只是跟改動前一樣，不會更糟。

壓力測試已驗證：日期完全交錯時，資料與整表掃描**逐筆一致**，全寬讀取每頁至多一次。

## 資料成長的擴充性

名單人數固定（41 人），所以每天筆數不變，成長的是天數。
壓力測試用同樣密度（約 20 筆/天）推到 2000 / 5000 / 10000 列：

| 資料量 | 涵蓋天數 | 45 天視窗回傳列 | 讀取格數 vs 整表 | 取內容 getRange 次數 |
|---|---:|---:|---|---:|
| 2000 | 120 | 約 750 | 遠低於整表 | ≤ 9 |
| 5000 | 250 | 約 900 | 遠低於整表 | ≤ 9 |
| 10000 | 500 | 約 900 | 遠低於整表 | ≤ 9 |

**教練後台的讀取量與「總列數」脫鉤，只與「視窗天數」相關。**
資料從 2000 成長到 10000 筆時，教練操作讀取的資料量幾乎不變。

## 未達成的部分（誠實記錄）

- **45 天視窗仍需讀取 44% 的格數**。要再往下壓必須把視窗收到 14~21 天，
  但那會影響 `applyReadiness` 對久未回報選手「最近 2 筆」的判斷。
  這是資料正確性 vs 效能的取捨，**本次選擇維持 45 天**，把常數留在
  `COACH_WINDOW_DAYS` 供日後調整。
- 本文件所有數字為**離線試算**（依真實 Sheet 內容與 Apps Script 的讀取語意計算），
  不是線上端對端計時。Apps Script 每次呼叫本身的固定成本實測為 **1.3–2.7 秒**，
  與資料量無關，那部分不因本次修改而改變。

---

# 2026-09-04｜Summary First / Detail On Demand（「上次表現」首頁）

「上次表現」首頁只是要畫一張名單，卻為此讀 14 天的 records。
改成 `getDailyAthleteSummary`（1 天摘要）+ 點選才讀明細。

## 實測（tests/summary-scale.test.js，名單人數固定、天數隨資料量成長）

| 情境 | CURRENT KB | NEW KB | CUR 列 | NEW 列 | CUR 格數 | NEW 格數 |
|---|---:|---:|---:|---:|---:|---:|
| 51 人 / 2000 列 | 353.2 | **7.3** | 750 | 50 | 127,164 | **14,364** |
| 51 人 / 5000 列 | 356.5 | **7.3** | 757 | 50 | 134,312 | **23,364** |
| 51 人 / 10000 列 | 358.4 | **7.3** | 761 | 50 | 144,968 | **38,364** |
| 51 人 / 20000 列 | 360.1 | **7.3** | 763 | 50 | 165,296 | **68,364** |
| 100 人 / 10000 列 | 704.1 | **14.4** | 1500 | 100 | 266,164 | **46,564** |
| 100 人 / 20000 列 | 705.6 | **14.4** | 1500 | 100 | 286,164 | **76,564** |

**payload 降為 1/48**（353 KB → 7.3 KB），**回傳列數降為 1/15**。

用正式 Sheet 的當日最多人數（33 人）實測，摘要 payload 為 **4.7 KB（gzip 0.6 KB）**。

## 請求次數

| 操作 | CURRENT | NEW |
|---|---:|---:|
| 首次開頁 | 2（records + traits，且 traits 阻塞名單） | **1**（摘要；traits 改背景載入） |
| TTL 內重新進頁 | 2 | **0** |
| 離開再返回 | 2 | **0** |
| 切換日期 | 2 | **1** |
| 點一位選手 | 0（資料已在首頁抓過） | **1**（明細） |
| 點 A → 返回 → 再點 A | 0 | **0**（TTL 內） |

首頁少一支、且快取命中時完全不打後端；代價是「點選手」多一支 —— 但那是使用者**主動**觸發、且只讀該選手。

## 一個沒有變快的地方（誠實記錄）

名單人數固定時，14 天視窗的**列數**本來就不會隨資料量成長，
所以 CURRENT 的 payload 從 2000 列到 20000 列只從 353 KB 變成 360 KB。
真正隨資料量線性成長的是**讀取格數**（127K → 165K），因為索引掃描要掃過每一列。
摘要同樣要掃索引，所以它的格數也會成長（14K → 68K），只是基數低很多。

**要讓讀取格數與總列數脫鉤，唯一的辦法是在 Sheet 上建立日期索引**，
那是另一個層級的改動，本次未做。
